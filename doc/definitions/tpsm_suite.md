# Third Party Service Manager - TPSM test suite

The `Third Party Service Manager - TPSM` monitors third party services in `OpenSync` and restarts them when they crash.
The principle of operation is the same as Diagnostic Manager - DM.
